<% content_for :header do -%>
   Listing all patient records.
<%end-%>

<div id = 'top-links'>
   <div class = 'right'>
      <%= link_to("<span>New patient record </span>", new_pms_patient_url, :class => 'buttonAdd', :title => 'Create New Patient')  %>
    </div>

    <div class = 'clear'></div>
  </div>
  <%= render :partial=> 'filters'%>
  <div id='patients-list'>
    <%=render :partial=> 'patients_list'%>
  </div>

