<% content_for :header do -%>
   Listing All Patient Records.
<%end-%>

<div id = 'top-links'>
   <div class = 'right'>
      <%= link_to("<span>Create New Patient </span>", new_pms_patient_url, :class => 'buttonAdd', :title => 'Create New Patient')  %>
    </div>

    <div class = 'clear'></div>
  </div>
  <%= render :partial=> 'filters'%>
  <br />
  <div id='patients-list'>
    <%=render :partial=> 'patients_list'%>
  </div>

