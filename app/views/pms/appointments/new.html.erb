<% form_for :appointment, :url => pms_appointments_path, :html => {:id => 'main_form'} do |f| %>
   <div class="form-wrapper">
     <%= error_messages_for 'appointment', 'patient' %>
     <div id="new-booking">

     	<div class='head'>New Appointment</div>
        <br />
        <%=render :partial => 'appointment_fields', :locals => {:f => f} %>
<% end %>

        <div id='patient_search' style="display:none">
           <%=render :partial => 'patient_search'%>
        </div>

     </div> <!--new booking-->

     <div id="main-schedules">
         <div class='head'>Existing Appointment details</div>
         <%= render :partial => 'schedules' %>
     </div>
  </div>
