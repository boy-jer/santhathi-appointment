<h1>Edit contact group</h1>

<% form_for([:admin,@contact_group]) do |f| %>
  <%= f.error_messages %>

  <p>
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </p>
  <p>
    <%= f.submit 'Update' %>
  </p>
<% end %>

<%= link_to 'Show', admin_contact_group_path(@contact_group) %> |
  
<%= link_to 'Back', admin_contact_groups_path %>
