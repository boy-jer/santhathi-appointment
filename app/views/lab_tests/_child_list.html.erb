 <li>
     <%=radio_button_tag('parent_id', service.id, checked = false, options = {:onclick => "$('main_form').lab_test_parent_id.value=#{service.id};"})%> <%= service.name %> 
      <% unless service.children.empty? %>
        <ul><%= render :partial => 'child_list', :collection => service.children, :as => :service %></ul>
      <% end %>  
 </li> 
