<% form_tag(laboratory_samples_url, :method => :get, :id => "search_form") do %>
   <table class="search-table">
     <tr>
       <th>Sample Name </th> <th>Used In</th>
       <th rowspan='2'><input type="reset" value="Reset"> </th>
     </tr>
     <tr>
       <%fields_for @search do |f|%>
          <% f.fields_for @search.conditions do |p| %>
              <td><%= p.text_field :sample_name_like, :class => 'textbox' %></td>
              <td><%= p.text_field :used_in_diagnosis_of_like, :class => 'textbox' %></td>
          <%end%>
       <%end%>

       </tr>
  </table>
<% end %>

<%= observe_form("search_form", :frequency => 1.5,
                 :before => "Element.show('spinner')",
                 :success => "Element.hide('spinner')",
                 :url => laboratory_samples_url, :method => :get) %>

<%= image_tag("spinner.gif",
              :align => "absmiddle",
              :border => 0,
              :id => "spinner",
              :style =>"display: none;" ) %>
