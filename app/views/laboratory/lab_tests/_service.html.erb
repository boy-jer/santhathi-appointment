<li> <%=link_to( image_tag("file-icon.gif", :border => 0), edit_laboratory_lab_test_path(service)) %>
   <%= link_to(service.name, edit_laboratory_lab_test_path(service)) %>
   <% unless service.children.empty? %>
     <ul><%= render :partial => 'service', :collection => service.children %></ul>
   <% end %>
</li>
