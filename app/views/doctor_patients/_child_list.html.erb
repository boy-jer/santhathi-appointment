
      <%= link_to service.name, '#' %>
      <% unless service.children.blank? %>
        <p style="padding-left: 1.5em; background: #fff;"><%= render :partial => 'child_list', :collection => service.children, :as => :service %></p>
      <% end %>
